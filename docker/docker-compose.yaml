# version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        USER: '${USER:-perichat}'
        USER_ID: '${WWWUSER:-1000}'
        GROUP_ID: '${WWWGROUP:-1000}'
    image: perichat-backend
    container_name: perichat-backend
    restart: unless-stopped
    working_dir: /var/www/
    ports:
      - "8090:8080"
    volumes:
      - ../:/var/www
    extra_hosts:
      - "host.docker.internal:host-gateway"

  nginx:
    image: nginx:stable-alpine
    container_name: perichat-server
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ../:/var/www
      - ./nginx/development.conf:/etc/nginx/conf.d/default.conf
  db:
    image: mariadb:11.3
    container_name: perichat-db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./storage/mysql:/var/lib/mysql
